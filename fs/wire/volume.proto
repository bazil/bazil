syntax = "proto3";

package bazil.db;

option go_package = "wire";

message VolumeStorage {
  string name = 1;
  string backend = 2;
  string sharingKeyName = 3;
}

message VolumeConfig {
  // Must be exactly 64 bytes long.
  bytes volumeID = 1;

  repeated VolumeStorage storage = 2;
}
