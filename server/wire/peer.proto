syntax = "proto3";

package bazil.peer;

option go_package = "wire";

message Peer {
  uint32 id = 1;
}

message PeerStorageConfig {
  // later this may include quota style restrictions
}

message PeerStorage {
  // key is the backend
  map<string, PeerStorageConfig> backends = 1;
}
